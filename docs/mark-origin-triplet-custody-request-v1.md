# Custody request packet

Use the owner issue title `Mark source custody request: origin_triplet_witness_v1` and the exact body fields pinned in `docs/mark-origin-triplet-source-uris-v1.txt` to trigger the governed acquisition workflow.
